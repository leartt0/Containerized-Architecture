Application source is copied from Adria Reserve at build time.

Run from repository root:

```bash
./scripts/prepare-app.sh
```

Requires `adria-reserve-2` (or set `ADRIA_RESERVE_PATH`) in the parent directory.
